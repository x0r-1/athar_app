name: Athar App Unified Handler
on:
  repository_dispatch:
    types: [new_video_upload, update_like]

permissions:
  contents: write

jobs:
  process:
    runs-on: ubuntu-latest
    # ميزة الـ Concurrency هنا هي "السر"
    # بتخلي الأكشنز تقف في طابور (Queue) وتشتغل واحد ورا التاني بالترتيب
    concurrency: 
      group: athar-db-update
      cancel-in-progress: false 

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Execute Logic
        env:
          FILE_ID: ${{ github.event.client_payload.file_id }}
          VIDEO_TITLE: ${{ github.event.client_payload.video_title }}
          VIDEO_ID: ${{ github.event.client_payload.video_id }}
          ACTION: ${{ github.event.client_payload.action }}
          # استخدمنا github.event.action عشان يقرأ نوع الـ Dispatch صح
          EVENT_TYPE: ${{ github.event.action }}
        run: |
          if [ "$EVENT_TYPE" == "new_video_upload" ]; then
            echo "🚀 جاري إضافة فيديو جديد..."
            python update_json.py
          elif [ "$EVENT_TYPE" == "update_like" ]; then
            echo "❤️ جاري تحديث لايكات الفيديو رقم $VIDEO_ID..."
            python manage_likes.py "$VIDEO_ID" "$ACTION"
          else
            echo "❓ نوع الحدث غير معروف: $EVENT_TYPE"
          fi

      - name: Save Changes with Retry Logic
        run: |
          git config --global user.name 'Athar Bot'
          git config --global user.email 'bot@athar.com'
          
          # محاولة الحفظ والرفع بذكاء
          for i in {1..10}; do
            git add videos.json
            # لو مفيش تغييرات حقيقية اخرج بسلام
            git commit -m "Automated Update: ${{ github.event.action }}" || exit 0
            
            # السحب (Rebase) ثم الرفع
            if git pull --rebase -X theirs origin main && git push; then
              echo "✅ تم التحديث بنجاح!"
              exit 0
            else
              echo "⚠️ تعارض في البيانات، محاولة جديدة بعد 5 ثواني..."
              sleep 5
            fi
          done
