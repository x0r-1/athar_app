name: Update Video JSON
on:
  repository_dispatch:
    types: [new_video]

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    # هذا الجزء هو سر الحل: يمنع تداخل الأكشنات ويجعلها تعمل بالتتالي
    concurrency: 
      group: update-json
      cancel-in-progress: false 

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0 # مهم جداً لجلب كل التاريخ ومنع التعارض

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'
          
      - name: Run Update Script
        env:
          FILE_ID: ${{ github.event.client_payload.file_id }}
          VIDEO_TITLE: ${{ github.event.client_payload.title }}
        run: python update_json.py
        
      - name: Commit and Push changes
        run: |
          git config --global user.name 'GitHub Action'
          git config --global user.email 'action@github.com'
          
          git add videos.json
          # إذا لم يوجد تغيير فعلي، لا يتوقف الأكشن
          git commit -m "Auto-update videos.json: ${{ github.event.client_payload.title }}" || exit 0
          
          # استراتيجية الرفع الذكي: يحاول الرفع 5 مرات مع انتظار 10 ثوانٍ لو حصلت زحمة
          for i in {1..5}; do
            git pull --rebase -X theirs origin main && git push && break || sleep 10;
          done
